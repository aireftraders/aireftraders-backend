const express = require('express');
const router = express.Router();

// Placeholder for withdrawal routes
router.get('/', (req, res) => {
  res.send('Withdrawal routes placeholder');
});

module.exports = router;